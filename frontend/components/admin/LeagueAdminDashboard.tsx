import React from 'react';

const LeagueAdminDashboard: React.FC = () => {
  return (
    <div className="w-full" role="region" aria-label="League admin dashboard" tabIndex={0}>
      <h1>League Admin Dashboard</h1>
      {/* TODO: Add league list, create/edit, season manager, CSV export, etc. */}
    </div>
  );
};

export default LeagueAdminDashboard; 