# TODO

- [ ] Deploy the backend API after confirming it runs locally
  - Validate local run: configure environment, run the service, and exercise key endpoints
  - Choose a deployment target (AWS Lambda with API Gateway, Fly.io, Railway, or a container on ECS/Fargate)
  - Define CI/CD to build and deploy on main branch
  - Provision secrets and environment variables in the chosen platform

